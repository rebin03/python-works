# Define:
# 1) tuple_obj = (obj1, obj2, obj3, ......)
# 2) tuple_obj = tuple()


# We can store same and different types of object
# order is preserved
# duplicate value is allowed
# tuple is immutable

numbers = (1,2,3,4,5)

print(numbers)