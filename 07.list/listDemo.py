# Define:
# 1) list_obj = [obj1, obj2, obj3, ......]
# 2) list_obj = list()

# There is no size limit for list
# We can store same and different types of object
# order is preserved
# duplicate value is allowed
# list is mutable

mobiles = ["Samsung","Apple","Realme","Oppo","Vivo"]

print(mobiles[3])

mobiles[2] = "oneplus"
print(mobiles)